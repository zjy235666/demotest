#include <iostream>
#include "add.h"
//修改aas
int main(int argc, const char** argv) {
    // 使用标准命名空间
    using namespace std;

    // 输出测试信息
    cout << "test" << endl;

    // 直接输出结果，减少不必要的变量
    cout << "x + y = " << 1+1<<endl;

    return 0;
}
